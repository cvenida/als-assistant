services:
  # PHP Application Container
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: klass-pilot_app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    networks:
      - klass-pilot-net

  # Web Server (Nginx)
  webserver:
    image: nginx:alpine
    container_name: klass-pilot_webserver
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - klass-pilot-net
    depends_on:
      - app

  # Database (MySQL)
  mysql:
    image: mysql:8.0
    container_name: klass-pilot
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysqldata:/var/lib/mysql
    networks:
      - klass-pilot-net

networks:
  klass-pilot-net:
    driver: bridge

volumes:
  mysqldata:
    driver: local